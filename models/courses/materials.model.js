/**
 * TODO:
 * 
 * create course materials class
 * 
 * static createNewFileObject(courseID, title, fileURL)
 * static createNewRecordingObject(courseID, title, fileURL, thumbnail?, duration)
 * static addNewStudentSubmission(courseID, studentID, submissionID, fileObj{filename, fileURL})
 * static deleteCourseMaterial(materialID, type(recording, file));
 * static getCourseMaterialsByCourse(courseID)
 * static getCourseMatrialByMaterial(materialID)
 * static getCourseMaterialByCourseIDAndType(courseID, type=[file, recording])
 */